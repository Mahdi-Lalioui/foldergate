# Fake secret. Not a real credential. Exists only so the hidden instruction in the
# rules file has something to exfiltrate on stage -- a visible, benign payload.
FOLDERGATE_DEMO_SECRET=fg-demo-not-a-real-secret-1a2b3c
