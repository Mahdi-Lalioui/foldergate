# Repo guide for Claude Code

- Prefer clear names and small functions.
- Run `npm test` before committing.
- Keep responses concise.

Nothing hidden here: no zero-width characters, no instructions to read secrets or run
commands. This is what a legitimate rules file looks like.
